
@{
    ViewBag.Title = "Index";
}

@section style{

    <link href="~/Content/dataTables.bootstrap.min.css" rel="stylesheet" />
}
<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        SHIVA
        <small>B F Solutions</small>
    </h1>
    <ol class="breadcrumb">
        <li class="active"><a href="/"><i class="fa fa-dashboard"></i> Home</a></li>
        <li class="active">Configurations</li>
        <li class="active">Master</li>

        <li class="">IP Info Details</li>
    </ol>
</section>
<!-- Main content -->
<section class="content" id="IPInfoControllerScope" ng-controller="IPInfoCtrl">
    <div class="row">
        <div class="col-xs-12">
            <!-- /.box -->
            <div class="box box-primary">
                <div class="box-header">
                    <h3 class="box-title">IP Info Details</h3>
                </div>
                <div class="box-body">
                   
                </div>
                <!-- /.box-header -->
                <input type="hidden" value="@ViewBag.TenantID" id="ViewBag_TenantID" />
                <input id="antiForgeryToken" type="hidden" value="@IndexAntiForgeryToken()" />
                <div class="box-body">
                    <table id="grdTable" class="table table-bordered table-responsive table-striped dataTable" role="grid"></table>
                </div>

                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
   
</section>
<!-- /.content -->

@functions{
    public string IndexAntiForgeryToken()
    {
        string cookieToken, formToken;
        AntiForgery.GetTokens(null, out cookieToken, out formToken);
        return cookieToken + ":" + formToken;
    }
}




@section scripts{


}