
<div class="modal-dialog modal-lg">
    <div class="modal-content">

        <!--myForm.$valid && -->
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <h4 class="modal-title">Purchase Order Details</h4>
        </div>
        <div class="modal-body">
            <div class="row" ng-show="PurchaseOrderDetailsIndexViewModel.length > 0">
                <div class="col-md-12" style="overflow-x:auto;">
                    <table cellpadding="0" cellspacing="0" class="table table-striped table-responsive table-bordered table-condensed">
                        <thead>
                            <tr class="">
                                <th>Product</th>
                                <th>Received Quantity</th>
                                <th>Balance Quantity</th>
                                <th>Unit Of Measurment</th>
                            </tr>
                        </thead>
                        <tbody class="">
                            <tr ng-repeat="poItem in PurchaseOrderDetailsIndexViewModel" class="ng-scope">
                                <td ng-bind="poItem.ProductName"></td>
                                <td ng-bind="poItem.RecivedQuantity"></td>
                                <td ng-bind="poItem.BalanceQuantity"></td>
                                <td ng-bind="poItem.UnitOfMeasurementName"></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="row" ng-show="PurchaseOrderDetailsIndexViewModel.length <= 0">
                <div class="col-md-12" style="overflow-x:auto;">
                    <table cellpadding="0" cellspacing="0" class="table table-striped table-responsive table-bordered table-condensed">
                        <tr>
                            <td>There is no record to display....</td>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <span class="btn btn-sm btn-danger" ng-click="ShowConfirm('modal-viewPurchaseOrderDetails')">Close</span>
         </div>

    </div>
    <!-- /.modal-content -->
</div>
